// Simple test file for VextLang extension

fn main() -> void {
    println("Testing VextLang extension!");
    
    let x = 42;
    let y = 3.14;
    let message = "Hello, World!";
    
    if x > 40 {
        println("x is greater than 40");
    } else {
        println("x is not greater than 40");
    }
    
    for i in 1..5 {
        println(format!("Count: {}", i));
    }
    
    let result = add(x, 10);
    println(format!("Result: {}", result));
}

fn add(a: int, b: int) -> int {
    a + b
} 