{
    "name": "vextlang-server",
    "version": "0.0.1",
    "description": "VextLang Language Server",
    "scripts": {
        "compile": "tsc -b",
        "watch": "tsc -b -w"
    },
    "dependencies": {
        "vscode-languageserver": "^8.0.0",
        "vscode-languageserver-textdocument": "^1.0.12"
    },
    "devDependencies": {
        "@types/node": "16.x",
        "typescript": "^5.0.0"
    }
}
